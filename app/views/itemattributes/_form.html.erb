<%= form_for(@itemattribute) do |f| %>
  <% if @itemattribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@itemattribute.errors.count, "error") %> prohibited this itemattribute from being saved:</h2>

      <ul>
      <% @itemattribute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
